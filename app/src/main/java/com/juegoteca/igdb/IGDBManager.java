package com.juegoteca.igdb;

/**
 * Created by alvaro on 3/7/18.
 */

public class IGDBManager {

/*
    IGDBWrapper wrapper = new IGDBWrapper("93399d38ec9ce21f9fc25fd546c72532", Version.STANDARD, false);

    public void getGamesFilter(IGDBFilter filtro){

        Parameters params = new Parameters()
                .addFields("name,platform,genre")
                .addFilter(filtro.getTitulo())
                .addFilter(filtro.getPlataforma())
                .addFilter(filtro.getGenero())
                .addOrder("name:asc");

        wrapper.games(params, new OnSuccessCallback(){
            @Override
            public void onSuccess(JSONArray result) {
                // Do something with resulting JSONArray
                for(int i = 0; i< result.length();i++){
                    try {
                        JSONObject currentGame = result.getJSONObject(i);
                        currentGame.get("id");



                    } catch (JSONException e) {
                        //TODO
                        e.printStackTrace();
                    }
                }
            }

            @Override
            public void onError(Exception error) {
                // Do something on error
            }
        });
    }
*/


}
